import 'package:flutter/material.dart';

class NearestClinic extends StatelessWidget {
  const NearestClinic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
