import 'package:flutter/material.dart';

class NearestPharmacy extends StatelessWidget {
  const NearestPharmacy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
